import React from 'react'
import PropTypes from 'prop-types'
import {
  LinearProgress,
} from '@material-ui/core'

import useStyles from '../style/popular'

const Search = () => <LinearProgress />

Search.propTypes = {}

export default Search
